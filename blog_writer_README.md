# 블로그 원고 자동생성기

주제만 입력하면 제목 → 본문 → 이미지 프롬프트까지 만들어주는 개인용 로컬 도구입니다.
발행은 자동화하지 않으며, 완성된 원고를 복사해서 네이버 블로그/티스토리에 직접 붙여넣는 방식입니다.

## 처음 설치하기

1. Python 3.10 이상이 설치되어 있어야 합니다.
2. 이 폴더에서 터미널을 열고 아래 명령을 실행하세요.

```bash
pip install -r requirements.txt
```

3. `.env.example` 파일을 복사해서 `.env` 파일을 만들고, Gemini API 키를 넣어주세요.

```bash
cp .env.example .env
```

API 키는 https://aistudio.google.com/apikey 에서 발급받을 수 있습니다.

## 실행하기

```bash
streamlit run app.py
```

실행하면 자동으로 브라우저가 열리고, 그 화면에서 계속 사용하시면 됩니다.
`.env`에 키를 넣어두면 앱을 열 때마다 자동으로 입력되어 있어요. (넣지 않았다면 사이드바에 직접 입력해도 됩니다.)

## 사용 순서

1. 왼쪽에서 발행 예정 채널(네이버/티스토리)과 글쓰기 유형(홈판형/검색형)을 고릅니다.
2. 주제와 참고 내용을 입력하고 "제목 후보 만들기"를 누릅니다.
3. 마음에 드는 제목을 고르거나 직접 수정한 뒤 "본문 쓰기"를 누릅니다.
4. "본문에서 이미지 프롬프트 뽑기"를 누르면 [사진: ...] 지점마다 이미지 프롬프트를 만들어줍니다.
5. 두 가지 방법 중 골라서 이미지를 직접 받을 수 있습니다.
   - **AI 이미지 생성 (완전 무료, 키 불필요)**: Pollinations.ai를 사용합니다. 회원가입도 API 키도 필요 없고, 워터마크도 없습니다. 다만 익명 요청은 15초에 1번 정도로 속도 제한이 있어서 이미지 여러 장을 만들 때는 순서대로 조금씩 시간이 걸립니다.
   - **실사 스톡사진 검색 (Pixabay, 무료 키 필요)**: 진짜 사진이 필요할 때 씁니다. https://pixabay.com/api/docs/ 에서 회원가입 후 즉시 무료로 키를 받을 수 있고, 사용량 제한도 넉넉합니다. 발급받은 키를 사이드바나 `.env`의 `PIXABAY_API_KEY`에 넣어주세요.
6. 마음에 드는 이미지는 다운로드 버튼으로 저장한 뒤, 제목/본문과 함께 네이버 블로그나 티스토리 편집기에 직접 첨부합니다.

## 다음에 추가할 수 있는 기능 (원하시면 말씀해주세요)

- 네이버 데이터랩/검색 API로 실시간 인기 키워드 참고 기능
- 여러 개 주제를 한 번에 큐에 넣고 순서대로 생성
- 생성한 원고를 로컬 파일로 저장해서 나중에 다시 불러오기
